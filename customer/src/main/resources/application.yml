spring:
  application:
    name: CustomerApplication

server:
  port: 8080

eureka:
  client:
    service-url:
      defaultZone: http://eureka:8761/eureka
      fetch-registry: true
      register-with-eureka: true